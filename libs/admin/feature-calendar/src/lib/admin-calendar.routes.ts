import { Route } from '@angular/router';
import { AdminCalendarComponent } from './components/admin-calendar/admin-calendar.component';

export const adminCalendarRoutes: Route[] = [
  { path: '', component: AdminCalendarComponent },
];
