# Soft-launch: the site is private behind a Cloudflare Access gate, so all
# crawling is disallowed. At public launch, restore the crawl rules and sitemap
# reference documented in the go-live runbook.
User-agent: *
Disallow: /
